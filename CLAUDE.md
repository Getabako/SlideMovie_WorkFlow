# プロジェクトルール

## 必須ルール（絶対遵守）

### 0. ユーザー指定の尊重（最重要）
**ユーザーが指定したモデル名、パラメータ、設定値は絶対に勝手に変更しない。**

- Claudeの知識カットオフ以降に登場した新しいモデルが存在する可能性がある
- 「このモデル名は存在しない」と勝手に判断してはいけない
- エラーが出た場合のみユーザーに報告し、代替案を提案する
- 品質やコストを考慮してユーザーが選んでいるため、勝手な最適化は禁止

### 1. 画像生成のモデル使い分け（コスト管理）
画像生成時は必ず以下のルールに従う：

- **講師キャラクターを含む画像**（講義場面、説明シーンなど）
  - モデル: Gemini 2.5 Flash Image（NanoBanana）
  - 参照: `/presentations/character/` のキャラクター画像を使用
  - 講師: 塾頭高崎翔太

- **講師キャラクターを含まない画像**（概念図、アイコン、背景など）
  - モデル: Gemini 2.0 Flash（廉価版）
  - キャラクター参照不要

この使い分けはコスト管理の肝。すべてのスライド資料作成時に適用すること。

### 2. APIキー管理
- APIキーは `.env` ファイルから自動読み込み
- 毎回ユーザーに聞かない
- `.env` は `.gitignore` に含める

### 3. スライド画像生成
- スライド数を正確にカウント（`---`区切りで数える）
- 全スライド分の画像を生成すること
- image_prompts.json には全スライドのプロンプトを含める

### 3.5. Googleスライド背景画像の生成ルール（絶対遵守）

#### 生成方式（コスト優先の3段階フォールバック）
1. **第1優先：Googleスライド「画像作成サポート」→「スライド」モード**（無料）【操作重視】
   - 制限がかかるまではこちらを使用
   - 制限の検知方法：「作成」ボタン押下後、タブが「スライド」→「画像」に自動切替されたら制限中
   - **スライド上のテキストボックスを残した状態で実行する**（Geminiがスライドの内容を「見て」画像を生成するため、テキストが視覚的な参照になる）
   - プロンプトはデザイン指示のみでOK（テキスト内容はGeminiが自動で読み取る）
   - 生成後にテキストボックスを削除（二重表示防止）
2. **第2優先：Gemini API / NanoBananaPro（`gemini-3-pro-image-preview`）**（有料・制限時のみ）【プロンプト重視】
   - 「スライド」モードが制限中の場合のみ使用
   - 必ず制限を確認してからAPIに切り替える（API代節約のため厳守）
   - **APIはスライドを「見る」ことができないため、プロンプトにすべてを含める必要がある**
   - プロンプトには: デザインイメージ + 載せるテキスト内容 + レイアウト指示 を詳細に記述
   - `python scripts/generate_api_prompts.py <marp.md>` で Marpからapi_prompts.jsonを自動生成可能
   - `hybrid_generator.mjs` に `--api-prompts api_prompts.json` で渡す
3. **「画像」モードは使用禁止**: 英語のみの汎用画像が生成されるため品質が低い

#### 2つの戦略の違い
| | 操作重視（スライドモード） | プロンプト重視（API） |
|---|---|---|
| コスト | 無料 | 有料 |
| テキストボックス | 残す（参照用） | あってもなくても可 |
| プロンプト | デザイン指示のみ | テキスト+デザイン全部 |
| テキスト削除 | 生成後に削除 | 生成後に削除 |
| スクリプト | hybrid_generator.mjs | gemini_slide_generator.py / hybrid_generator.mjs (fallback) |

#### 品質基準（全スライド共通）
- **日本語のみ**: 英語のみの画像は不可。必ず日本語でスライド内容を記載
- **内容の忠実性**: スライドのテーマ・内容を正確に反映すること。汎用的・無意味な画像は不可
- **ワークスライド**: 記入欄・タイマー・具体的な指示を含むワークシート形式で生成
- **テキスト要素の削除**: 背景設定後、元のテキストボックスは必ず削除（二重表示防止）
- **既存画像要素の確認**: 背景設定前に、スライド上の既存画像要素を確認し削除（旧画像が上に重なる問題の防止）

#### 完了後の品質監査（必須）
- **全スライドのスクリーンショットを撮影**して目視確認
- 以下に該当するスライドは再生成：
  - 英語のみの画像（例："FAMILY ROOTS", "HOME VISIT NURSING"）
  - 内容が不足・無意味な画像（例：タイトルだけでワーク内容なし）
  - テキスト要素が残って二重表示になっている
  - 背景が表示されていない（旧画像要素がブロックしている）
- **一発で完了すること**: 中途半端な状態で完了報告しない

#### 使用スクリプト
- `scripts/hybrid_generator.mjs`: スライドモード→API自動フォールバック（メインスクリプト）
- `scripts/generate_api_prompts.py`: Marp MDからAPI用詳細プロンプト（api_prompts.json）を自動生成
- `scripts/gemini_slide_generator.py`: Gemini API画像生成→Driveアップロード→背景設定→テキスト削除
- `scripts/delete_text_elements.py`: テキスト要素の一括削除
- `scripts/set_slide_backgrounds.py`: 画像を背景に設定＋テキスト要素削除

### 4. 動画レンダリング
- チャンクレンダリング時は concat_list.txt の全チャンク数を確認
- 残りチャンクがあれば継続してレンダリング

### 5. スライド作成方式（新方式）
- **1ファイル方式**: NotebookLM分割（_1.md, _2.md...）は使わない。1つの.mdファイルに全スライドを記載
- **最低3つのワークを含める**: 聞くだけでなく手を動かす講座にする。概論→基礎→ワーク①→応用→ワーク②→実践→ワーク③→まとめの構成。ワークは参加者が実際に書き込み・練習できる内容にすること
- **AI/ICT系は最新情報をWeb検索で必ず確認**: 古い情報を最新と言わない
- **AI講座のワークではプロンプトを資料に記載**: 参加者がコピペできる形式で
- **詳細は `SLIDE_CREATION_GUIDE.md` を参照**

### 6. スライド内容の3人審査員レビュー（必須）
スライドの内容（原稿・構成）を作成したら、必ず以下の3人の審査員ペルソナでレビューし、改善した上で最終版を出力すること。

**審査員A：現場の辛口実践者**（100点満点）
- 専門用語に逃げていないか？
- 現場のリアルな悩みに寄り添っているか？
- 明日から使える具体的なアクションが提示されているか？

**審査員B：敏腕YouTubeプロデューサー**（100点満点）
- タイトルと冒頭2枚で強烈なフック（興味付け）があるか？
- 1枚あたりの情報量が適切で、視覚的に目を引く工夫があるか？
- 途中で飽きさせない「意外な事実」や「面白い例え話」が入っているか？

**審査員C：凄腕BtoBマーケター**（100点満点）
- 「自社（自分）に頼むべき理由・権威性」が自然に伝わるか？
- 視聴者が「プロのサポートが必要だ」と感じる構成か？
- サービスへの導線が「売り込み」ではなく「救済措置」として機能しているか？

**実行手順**
1. Step 1: 各審査員が辛口レビュー＋採点
2. Step 2: 指摘を統合した改善方針リスト（平均90点未満は大幅構成変更）
3. Step 3: 改善を全反映した最終版を出力

### 7. 最新情報の確認（AI/ICT系講座で必須）
- スライド作成前に必ずWeb検索で最新情報を収集
- AIモデル名・バージョン・料金・機能は公式情報を確認
- 不確実な情報は「○年○月時点」と明記
- トレンドでない情報をトレンドと表現しない

## ディレクトリ構造
```
presentations/
├── character/           # キャラクター画像（講師、ロゴ）
│   ├── 塾頭高崎翔太/
│   └── ロゴ/
├── [プレゼン名]/
│   ├── [プレゼン名].md  # Marpスライド
│   ├── images/          # 生成画像
│   └── image_prompts.json
```

## コマンド
- `/image-generate [プレゼン名]` - 画像生成
- `/image-insert [プレゼン名]` - 画像挿入
- `/video-create [プレゼン名]` - 動画作成
- `/youtube-upload [プレゼン名]` - YouTube投稿

## YouTube投稿機能

### 使い方
```bash
python scripts/upload_to_youtube.py [プレゼン名] [--privacy public|private|unlisted]
```

### 初回認証
初回実行時はブラウザでGoogleアカウント認証が必要。
認証後は `youtube_token.pickle` に保存され、次回以降は自動認証。

### 自動設定される項目
- **タイトル**: プレゼン名から自動生成（AI/IT/教育キーワード判定）
- **説明文**: チャンネル紹介、動画内容、CTA（いいね・登録促進）を含む
- **タグ**: 検索されやすいキーワードを自動設定
- **カテゴリ**: 教育（ID: 27）
- **言語**: 日本語

### 投稿先チャンネル
https://www.youtube.com/@if-juku
